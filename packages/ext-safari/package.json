{
  "name": "@umbra/ext-safari",
  "private": true,
  "version": "0.1.0",
  "description": "Umbra's Safari Web Extension shell — a macOS Xcode project wrapping the shared web-extension bundle, plus the resource-sync build script.",
  "license": "MIT",
  "author": "Ashwin Sathian",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AshwinSathian/umbra.git",
    "directory": "packages/ext-safari"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node build.mjs",
    "build:xcode": "node build.mjs --build"
  }
}
